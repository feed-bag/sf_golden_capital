<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Submitted_Requires_Account</fullName>
    <active>true</active>
    <description>Account Name must be populated to move to Submitted.</description>
    <errorConditionFormula>AND(
  ISPICKVAL(StageName, "Submitted"),
  ISBLANK(AccountId)
)</errorConditionFormula>
    <errorDisplayField>AccountId</errorDisplayField>
    <errorMessage>Account Name is required before moving to Submitted.</errorMessage>
</ValidationRule>
