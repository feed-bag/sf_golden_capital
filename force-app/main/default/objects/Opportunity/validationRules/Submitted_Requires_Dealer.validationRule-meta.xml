<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Submitted_Requires_Dealer</fullName>
    <active>true</active>
    <description>Dealer is required to move to Submitted unless the deal is marked as Private Party.</description>
    <errorConditionFormula>AND(
  ISPICKVAL(StageName, "Submitted"),
  ISBLANK(Dealer__c),
  NOT(Private_Party__c)
)</errorConditionFormula>
    <errorDisplayField>Dealer__c</errorDisplayField>
    <errorMessage>Dealer is required before submitting. If this is a private party deal, check the Private Party checkbox.</errorMessage>
</ValidationRule>
